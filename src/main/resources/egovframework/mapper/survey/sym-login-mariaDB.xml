<?xml version="1.0" encoding="UTF-8" ?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="egovframework.sym.login.dao.LoginDAO"> 

	
	<select id="selectUser" parameterType="egovMap" resultType="egovMap">
		<![CDATA[
			SELECT * FROM TN_SRV_USER WHERE USER_ID = #{usrid}
		]]>
	</select>

</mapper>

